[package]
name = "lam-redb"
version.workspace = true
edition.workspace = true
description = "Durable redb state-store implementation for lam"
readme = "README.md"

[dependencies]
lam-core = { path = "../lam-core" }
redb.workspace = true
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
lam-core = { path = "../lam-core", features = ["test-support"] }
tempfile.workspace = true
tokio.workspace = true

[lints]
workspace = true
